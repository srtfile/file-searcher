services:
  - type: web
    name: advanced-file-search-web
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: AUTH_TOKEN
        generateValue: true
      - key: SEARCH_ROOT
        value: /opt/render/project/src/search_root
